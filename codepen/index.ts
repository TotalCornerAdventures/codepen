// A simple TypeScript program that adds two numbers and prints the result

function add(a: number, b: number): number {
  return a + b;
}

const result = add(2, 3);
console.log("The sum is:", result);

add(5, 7);
